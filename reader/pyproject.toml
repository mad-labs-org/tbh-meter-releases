[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-v --tb=short"

[tool.coverage.run]
source = ["metrics", "shared", "config"]
omit = ["tests/*", "scripts/*"]

[tool.coverage.report]
show_missing = true
